#include "Robot.h"
#include "RobotState_Seek.h"
#include <map>
int main()
{
	std::map<StateType,RobotState*> startingState;
	//startingState[StateType::SEEK, ];
	//Robot* robot = new Robot(StateType::SEEK, );

	return 0;
}